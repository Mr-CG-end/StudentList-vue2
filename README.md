# Vue 2 学生管理系统 (Student Management System)

> 🎯 一个专为 Vue 2 初学者设计的练习项目，旨在帮助你掌握 Vue 2 的核心概念（Options API）以及 Element UI 的基本使用。

## 🌐 在线演示 (Live Demo)

**👉 [点击这里查看在线演示](https://mr-cg-end.github.io/StudentList-vue2/)**

[![GitHub Pages](https://img.shields.io/badge/GitHub%20Pages-部署成功-success?logo=github)](https://mr-cg-end.github.io/StudentList-vue2/)
[![Vue](https://img.shields.io/badge/Vue-2.7-brightgreen?logo=vue.js)](https://v2.vuejs.org/)
[![Element UI](https://img.shields.io/badge/Element%20UI-2.15-blue?logo=element)](https://element.eleme.cn/)

## 🛠 技术栈 (Tech Stack)

- **Vue Framework**: Vue 2.7
- **UI Component**: Element UI 2.15.x
- **Router**: Vue Router 3.x
- **Build Tool**: Vue CLI / Webpack

## 🚀 快速开始 (Getting Started)

在开始之前，请确保你的电脑上已经安装了 Node.js。

### 1. 安装依赖 (Install Dependencies)

在项目根目录下打开终端（Terminal），运行以下命令安装项目所需的依赖包：

```bash
npm install
```

### 2. 启动开发服务器 (Run Dev Server)

安装完成后，运行以下命令启动项目：

```bash
npm run serve
```

启动成功后，控制台会显示访问地址（通常是 `http://localhost:8080`），在浏览器中打开该地址即可看到项目。

## 📂 项目结构 (Project Structure)

```
src/
├── components/
│   └── MainLayout.vue    # 核心布局组件（侧边栏 + 顶部栏 + 内容区）
├── router/
│   └── index.js          # 路由配置文件
├── views/
│   ├── Login.vue         # 登录页面 (表单验证)
│   ├── Dashboard.vue     # 首页/仪表盘
│   └── StudentList.vue   # 学生列表页 (表格、增删改查、弹窗)
├── App.vue               # 根组件
└── main.js               # 入口文件 (引入 Element UI, Router 等)
```

## ✨ 功能特性 (Features)

- ✅ **学生信息管理**：实现学生信息的增删改查功能
- ✅ **数据展示**：使用 Element UI 表格组件展示学生列表
- ✅ **表单验证**：登录页面包含完整的表单验证逻辑
- ✅ **路由导航**：支持多页面切换（登录、仪表盘、学生列表）
- ✅ **响应式布局**：基于 Element UI 的响应式设计
- ✅ **弹窗交互**：使用对话框组件完成编辑和添加操作
- 🔜 **数据搜索**：学生信息搜索与筛选（计划中）
- 🔜 **数据导出**：支持导出学生数据为 Excel（计划中）

## 📝 学习重点 (Learning Points)

在这个项目中，你可以练习以下内容：

1.  **组件通信**: 学习父子组件如何配合工作（如 `MainLayout` 包裹 `router-view`）。
2.  **Vue Router**: 学习路由配置、嵌套路由（`children`）、路由跳转（`this.$router.push`）。
3.  **Element UI**:
    - `<el-table>`: 表格数据的展示与自定义列模板 (`slot-scope`)。
    - `<el-form>`: 表单的数据绑定 (`v-model`) 与验证。
    - `<el-dialog>`: 弹窗的控制 (`visible.sync`)。
    - `<el-menu>`: 侧边栏导航菜单的使用。
4.  **Options API**: 熟练掌握 `data`, `methods`, `created`, `computed` 等选项的使用。

## 💡 下一步建议 (Next Steps)

- 尝试完善 `Login.vue` 中的登录逻辑，添加真实的表单验证规则。
- 尝试在 `StudentList.vue` 中添加“搜索”功能。
- 尝试将数据操作（增删改查）封装到一个单独的 API 模块中，模拟真实的后端调用。

祝你学习愉快！
